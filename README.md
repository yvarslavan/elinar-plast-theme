# 📦 Документация проекта: Elinar Plast

> Корпоративный B2B-сайт производственной компании **ООО «Элинар Пласт»**
> Сайт: [https://elinar-plast.ru](https://elinar-plast.ru)

---

## 1. Общая информация

| Параметр              | Значение                                      |
|-----------------------|-----------------------------------------------|
| **Название проекта**  | Elinar Plast — корпоративный сайт             |
| **Тема WordPress**    | `elinar-plast` (кастомная, не дочерняя)       |
| **Версия темы**       | 1.0                                           |
| **Версия WordPress**  | 6.x (рекомендуется 6.4+)                      |
| **Версия PHP**        | 8.1+ (рекомендуется 8.2)                      |
| **База данных**       | MySQL 8.0 / MariaDB 10.6+                     |
| **Локальная среда**   | Local by Flywheel (Windows)                   |
| **Репозиторий**       | GitHub (папка темы находится под контролем Git) |

---

## 2. Используемые плагины

| Плагин                        | Назначение                                                                 |
|-------------------------------|----------------------------------------------------------------------------|
| **All-in-One WP Migration**   | Создание резервных копий и перенос сайта целиком (файлы + БД в одном архиве) |
| **WP Mail SMTP**              | Надёжная отправка email через SMTP-сервер (настроен для `plast@elinar.ru`) |

> **Важно:** Уведомления о заявках с форм сайта отправляются **двумя параллельными каналами**: по email (через WP Mail SMTP) и в Telegram-бот. Оба канала настроены в `functions.php`.

---

## 3. Структура папки `wp-content`

```
wp-content/
├── themes/
│   └── elinar-plast/          ← Папка темы (основная рабочая зона)
│       ├── assets/            ← Все статические ресурсы темы
│       │   ├── css/           ← Модульные CSS-файлы (по одному на страницу/компонент)
│       │   ├── js/            ← Модульные JS-файлы (по одному на страницу/компонент)
│       │   ├── images/        ← Изображения темы (hero, логотипы, фоны)
│       │   ├── fonts/         ← Локальные шрифты Inter и Manrope (опционально)
│       │   ├── icons/         ← SVG-иконки
│       │   └── video/         ← Видеофайлы (фоновое видео страницы «О нас»)
│       ├── template-parts/    ← Переиспользуемые части шаблонов (подключаются через include)
│       ├── style.css          ← Главный файл стилей (содержит заголовок темы + все базовые стили)
│       ├── functions.php      ← Ядро темы: регистрация скриптов, обработка форм, AJAX, Telegram
│       ├── header.php         ← Шапка сайта (выводится на всех страницах)
│       ├── footer.php         ← Подвал сайта (выводится на всех страницах)
│       ├── front-page.php     ← Шаблон главной страницы
│       ├── page-about.php     ← Страница «О компании»
│       ├── page-products.php  ← Страница «Продукция»
│       ├── page-services.php  ← Страница «Наши услуги»
│       ├── page-contacts.php  ← Страница «Контакты»
│       ├── page-technologies-production.php ← Страница «Производство»│       ├── page-quote-request.php ← Страница детальной заявки на расчёт производства
│       ├── page-thank-you.php ← Страница «Спасибо» (после отправки формы)
│       └── ...                ← Прочие вспомогательные шаблоны
│
├── plugins/
│   ├── all-in-one-wp-migration/  ← Плагин резервного копирования
│   └── wp-mail-smtp/             ← Плагин SMTP-почты
│
└── uploads/                   ← Медиафайлы, загруженные через панель WordPress
    └── (год)/(месяц)/         ← Стандартная структура WordPress Media Library
```

---

## 4. Ключевые файлы темы

### `style.css` — Главный файл стилей
Содержит **заголовок темы** (обязательный для WordPress) и **все базовые стили**: CSS-переменные (цвета, шрифты, отступы), сброс стилей, типографику, сетку, компоненты кнопок, стили хедера и футера, адаптивность.

Дополнительные стили вынесены в модульные файлы в `assets/css/` и подключаются только на нужных страницах для оптимизации производительности.

---

### `header.php` — Шапка сайта
Выводится на **всех страницах** через `get_header()`. Содержит:
- `<head>` с мета-тегами, preconnect к Google Fonts, preload критических шрифтов и hero-изображений (оптимизация LCP)
- **Инлайн Critical CSS** — минимальные стили для мгновенного отображения hero-блока без мерцания
- **GSAP Ready Wrapper** — очередь инициализации анимаций, ожидающих загрузку библиотеки GSAP
- **Фикс для Opera Android** — инлайн-скрипт, исправляющий отображение SVG-иконок на этом браузере
- Фиксированный хедер с логотипом (два варианта: белый и цветной — переключаются при скролле)
- Главное навигационное меню (`primary`) с выпадающими подменю
- Иконки быстрой связи: телефон (открывает модальное окно обратного звонка) и Telegram
- **Хлебные крошки** — выводятся на всех страницах, кроме главной

---

### `footer.php` — Подвал сайта
Выводится на **всех страницах** через `get_footer()`. Содержит:
- **Pre-footer блок** «Инжиниринг полного цикла» — полосу с ключевым УТП перед подвалом
- **Четырёхколоночный футер**: описание компании с адресом, навигация по продукции, контакты (телефоны, email, часы работы), блок CTA «Запросить расчёт»
- **Модальные окна продуктов** (6 штук: ПВХ-термовставки, фаскообразователи, профили для шинопровода, профили для бытовой техники, полимерные втулки, профили для автофургонов) — доступны на всех страницах
- **Плавающая кнопка связи** (FAB) — раскрывающийся виджет с иконками Telegram, звонка и email
- **Модальное окно обратного звонка** — форма с полем телефона
- Нижняя строка с копирайтом, ссылками на политику конфиденциальности и реквизитами

---

### `front-page.php` — Главная страница
Шаблон главной страницы. Подключается WordPress автоматически, если назначена статическая главная страница. Состоит из секций:
1. **Hero** — полноэкранный блок с адаптивным фоновым изображением (WebP, разные версии для mobile/tablet/desktop), заголовком и кнопками CTA
2. **Слайдер производства** — интерактивный слайдер с фотографиями производственного процесса (`template-parts/production-slider.php`)
3. **Блок статистики** — анимированные счётчики роста компании за 10 лет
4. **Ключевые направления** — блок с технологиями экструзии и литья (`template-parts/key-directions.php`)
5. **Сетка продуктов** — 6 карточек с кратким описанием и кнопкой «Подробнее», открывающей модальное окно
6. **FAQ-баннер** — промо-блок со ссылкой на раздел FAQ страницы услуг
7. **Блок преимуществ** — 6 карточек «Почему выбирают нас» с SVG-иконками
8. **Производственный цикл** — визуализация этапов работы (`template-parts/production-cycle.php`)
9. **Форма инженерной оценки** — форма заявки с загрузкой файлов (`template-parts/audit-form-section.php`)

---

### `functions.php` — Ядро функционала темы
Центральный файл темы (~2900 строк). Ключевые блоки:

| Блок | Описание |
|------|----------|
| **Константы конфигурации** | Флаги включения/отключения оптимизаций (локальные шрифты, defer скриптов, preload и т.д.) |
| **Обработчик форм** | `elinar_handle_project_form_universal()` — универсальный POST-обработчик формы «Запросить инженерную оценку» на всех страницах. Включает CSRF-защиту (nonce), honeypot от спама, валидацию полей, загрузку файлов (PDF, DWG, DXF, STEP, STL и др., до 5 файлов по 15 МБ) |
| **Telegram-уведомления** | `elinar_send_telegram_notification()` — отправка заявок в Telegram-канал параллельно с email. Токен бота задаётся константой `TELEGRAM_BOT_TOKEN` в `wp-config.php` |
| **Обработчик Quote Form** | Отдельный обработчик для детальной формы расчёта производства (`/quote-request/`) с полями: технология (экструзия/литьё), материал, габариты, объём, сроки, контакты |
| **Подключение скриптов** | Умная загрузка CSS/JS: каждая страница получает только нужные ей файлы. Скрипты подключаются с `defer` для ускорения загрузки |
| **Оптимизация производительности** | Preconnect к CDN шрифтов, preload hero-изображений, отложенная загрузка некритических стилей |
| **Меню** | Регистрация позиции `primary` для главного навигационного меню |
| **Хлебные крошки** | Кастомная функция генерации хлебных крошек без плагинов |
| **Favicon** | Программная установка favicon через `wp_site_icon` |
| **Аналитика** | Подключение счётчиков (Яндекс.Метрика и др.) |

---

## 5. Инструкция по развёртыванию на новом сервере

### Вариант А — Через All-in-One WP Migration (рекомендуется)

1. В панели WordPress на текущем сервере: **All-in-One WP Migration → Export → File**
2. Скачайте архив `.wpress`
3. На новом сервере установите чистый WordPress
4. Установите плагин **All-in-One WP Migration**
5. **All-in-One WP Migration → Import** → загрузите архив `.wpress`
6. После импорта обновите URL сайта: **Настройки → Общие** → укажите новый домен
7. Сбросьте постоянные ссылки: **Настройки → Постоянные ссылки → Сохранить**

### Вариант Б — Ручное развёртывание

1. **Скопируйте файлы** из архива в корневую директорию сайта на сервере (обычно `/var/www/html/` или `/public_html/`)
2. **Создайте базу данных** MySQL и пользователя с полными правами на неё
3. **Импортируйте дамп БД** (файл `.sql`):
   ```bash
   mysql -u [пользователь] -p [имя_базы] < backup.sql
   ```
4. **Отредактируйте `wp-config.php`** в корне сайта — укажите данные новой БД:
   ```php
   define('DB_NAME',     'имя_базы');
   define('DB_USER',     'пользователь');
   define('DB_PASSWORD', 'пароль');
   define('DB_HOST',     'localhost');
   ```
5. **Обновите URL сайта** в БД (если домен изменился):
   ```sql
   UPDATE wp_options SET option_value = 'https://elinar-plast.ru' WHERE option_name = 'siteurl';
   UPDATE wp_options SET option_value = 'https://elinar-plast.ru' WHERE option_name = 'home';
   ```
6. Сбросьте постоянные ссылки через панель WordPress

### Настройка Telegram-уведомлений

Добавьте в `wp-config.php` (до строки `/* That's all, stop editing! */`):
```php
define('TELEGRAM_BOT_TOKEN', 'ВАШ_ТОКЕН_БОТА');
define('ELINAR_PRIMARY_EMAIL', 'plast@elinar.ru');
define('ELINAR_COPY_EMAIL',    'ваш@email.ru');
```

---

## 6. Доступы

> ⚠️ **Внимание:** Храните эти данные в надёжном месте. Не передавайте третьим лицам.

### Панель администратора WordPress

| Параметр | Значение |
|----------|----------|
| URL входа | `https://elinar-plast.ru/wp-admin/` |
| Логин | yvarslavan |
| Пароль | Lalee1966 |

### Хостинг cPanel

| Параметр | Значение |
|----------|----------|
| URL входа | `https://elinar-plast.ru/cpanel/` (https://scp117.hosting.reg.ru:2083) |
| Логин | u0109603 |
| Пароль | !6TZmXEq7cV7!h |

### FTP / SFTP доступ к серверу

| Параметр | Значение |
|----------|----------|
| Хост | ftp.u0109603.cp.regruhosting.ru | IP-адрес сервера: 37.140.192.138
| Порт | `21` / `22` |
| Логин | u0109603 |
| Пароль | !6TZmXEq7cV7!h |
| Путь к сайту | `/var/www/u0109603/public_html/wp-content/themes/elinar-plast/` |

### База данных

| Параметр | Значение |
|----------|----------|
| Хост | `localhost:3306` |
| Имя БД | `u0109603_wp` |
| Пользователь | `u0109603_wp` |
| Пароль | FlvPsd366120801991& |
| phpMyAdmin | https://scp117.hosting.reg.ru:2083/cpsess4680858213/3rdparty/phpMyAdmin/index.php?lang=ru |

### Telegram-бот (уведомления о заявках)

| Параметр | Значение |
|----------|----------|
| Токен бота | 7672102144:AAFM47VmlL1otzZljLXG8mXq-rebxxf1PSw |
| Telegram-канал | `-1003410037262` |

### Хостинг / Панель управления

| Параметр | Значение |
|----------|----------|
| Провайдер | reg.ru |
| URL панели | https://reg.ru |
| Логин | Elinarplast |
| Пароль | kCPtUcwzRpcDrJcru2#hu%N3 |

---

## 7. Контакты разработчика

По всем техническим вопросам, связанным с сайтом, обращайтесь:

| | |
|-|-|
| **Разработчик** | Юрий |
| **Email** | varslavanyury@gmail.com |
| **Telegram** | https://t.me/yury_var |

---

*Документ подготовлен: февраль 2026 г.*
